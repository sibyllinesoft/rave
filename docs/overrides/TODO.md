# Override Layer TODO

- [x] Add CLI tooling for creating/managing multiple override layers with explicit priorities.
- [x] Integrate a remote-aware dry-run that previews changes/restarts before applying overrides.
- [x] Provide sample override files + enriched metadata guidance so users can learn the pattern quickly.
